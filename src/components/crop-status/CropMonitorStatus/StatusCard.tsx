import * as React from 'react';
import { StatusCardProps } from '../types';

export const StatusCard: React.FC<StatusCardProps> = ({ title, value, description, imageSrc, color }) => {
  // 这里放入你的 StatusCard 组件代码
}; 