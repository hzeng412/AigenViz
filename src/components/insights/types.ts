export interface TimelineItem {
    status: string;
    date: string;
    color: string;
  }
  
  export interface PressureIndicator {
    color: string;
    title: string;
    description: string;
  }
  
  export interface GalleryImage {
    src: string;
    date: string;
    alt: string;
  }